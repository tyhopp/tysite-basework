// Export a createPages function that:
// - Gets page data
// - Adds the route information to the routes file
// - Gets the page template to use
// - Adds a script tag to run when the page is loaded
// - Adds all meta data
// - Create the [page]-data.json file for the page
// - Creates the html file for the page

const createSubPages = async () => {
  console.log('Created sub page');
}

module.exports = {
  createSubPages
}